export interface IBlog {
    _id: string
    headline: string
    thumbnail: string
    category: string
    tags: string[]
    content: string 
    status: string
    createdAt: string
    updatedAt: string
}